# Blue Avenue Groove Homesite

**This is the homesite for Blue Avenue Groove - built with Next.js and React**

## How to run

1. Run `yarn` to install dependencies
2. Run `yarn dev` to start the development server
3. Run `yarn build` to create a production build (static export to `out/`)
